import React from 'react';

function Age({ age, name }) {
    return (
        <div>
            <span> Age: {age}</span>
            <span> Name: {name}</span>
        </div>
    );
}

export default Age;
